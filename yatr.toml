# Generated by yatr rollout — adjust as needed.

[tasks.bootstrap]
run = ["npm run bootstrap"]

[tasks.prepare]
run = ["npm run prepare"]

[tasks.release]
run = ["npm run release"]

[tasks.test]
run = ["npm run test"]

[tasks."test:ci"]
run = ["npm run test:ci"]

[tasks.clean]
run = ["npm run clean"]

[tasks.compile]
run = ["npm run compile"]

[tasks.ci]
desc = "CI pipeline"
depends = ["test"]
